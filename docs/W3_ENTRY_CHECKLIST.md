# W3 진입 체크리스트 (수동 확인분)

> 자동 EditMode 테스트 58개는 전부 그린(2026-07-28 기준). 아래는 **테스트로 못 덮는 수동 항목**만 모은 것.
> 이 표가 전부 체크돼야 W3(화면 #12~#16) 진입 가능. 게이트 근거: `IMPLEMENTATION_PLAN.md` "W3 진입 조건" + 프로토콜 51번(closed는 1차 신호일 뿐, 수동 기준까지 실제 통과해야 함).

## 자동 테스트로 이미 검증된 것 (참고 — 재확인 불필요)

- `#7` RuleEvaluator JSON 실행 / 스태미나 0 폴백 → `RuleEvaluatorTests` (7)
- `#8` EventLog 생성 / rule_id 포함 → `EventLogTests` (5)
- `#10` 거절·되묻기·모순 케이스 매핑 → `TrainingCompilerTests` (7)
- `#11` 프롬프트 인젝션 거절 → `PromptInjectionDefenseTests` (9) ✅ 완료

## 수동 확인 항목 (남은 게이트)

| # | 담당 | 확인할 것 | 방법 | 합격 기준 | 완료 |
|---|---|---|---|---|---|
| 1 | A(KJ) | #7 규칙셋 바꾸면 전투 행동이 바뀐다 | 메뉴 `TeachAndFight/Combat/RuleEvaluator 검증 - JSON 규칙셋 매치 시뮬레이션` 실행 → 규칙셋 JSON을 다른 걸로 바꿔 재실행 | 규칙셋에 따라 행동/로그가 눈에 띄게 달라짐 | ☐ |
| 2 | A(KJ) | #7 스태미나 0일 때 다음 규칙으로 폴백 | 위 시뮬레이션에서 스태미나 소진 상황 재현 | 스태미나 부족 규칙을 건너뛰고 다음 규칙 발동 | ☐ |
| 3 | A(KJ) | #8 경기 1회 후 EventLog가 정상 생성되고 rule_fired에 rule_id 포함 | 매치 1회 실행 후 로그 객체/파일 육안 확인 | 엔트리 존재 + 각 rule_fired에 유효한 rule_id | ☐ |
| 4 | B(JM) | #10 자연어→유효 diff **10회 중 9회 이상** | 메뉴 `TeachAndFight/LLM/훈련 컴파일 파이프라인 검증 (TrainingCompiler)`로 "상대가 궁 쓰면 대시로 피해" 류 입력 10회 | 유효 add diff 9회 이상 (비결정적이라 수기 기록) | ◐ 진행중 (실제 LLM 1/10 성공: 07-28 엔드투엔드 실행에서 Applied 확인, 9회 더 필요) |
| 5 | B(JM) | #10 04장 완료기준 4케이스 각각 동작 | 정상/거절("체력 0으로")/되묻기("잘 좀 싸워봐")/모순 입력 각 1회 | Applied / Rejected(또는 NeedsConfirmation) / NeedsConfirmation / conflict_with 지정 | ☐ |
| 6 | B(JM) | #10 API 키 없음·타임아웃 시 폴백 대사 | `ANTHROPIC_API_KEY` 제거 후 호출 | 게임 안 죽고 "죄송해요, 잘 못 알아들었어요…" 출력 | ☐ |
| 7 | **공용(A+B)** | **엔드투엔드 통합 시연** (필수) | 플레이어가 자연어로 규칙 하나 가르침 → RuleValidator 통과 → 실제 전투(RuleEvaluator)에 그 규칙이 발동되는 것을 A/B가 함께 확인 | 가르친 규칙이 실제 경기에서 발동 로그로 확인됨 | ✅ 07-28 통과 (실제 Haiku: "거리 멀면 다가가" → Applied → rule_01 전투 발동 9건. 임시 하네스 `TeachAndFight/Integration/엔드투엔드 검증`) |

## 주의

- 7번(엔드투엔드)이 안 되면, W3에서 만들 화면(#13~#15)은 미검증 데이터 흐름 위에 얹는 꼴 → 나중에 배관 사고. **화면 착수 전 반드시 통과.**
- 4번 "10회 중 9회"는 LLM 특성상 자동 CI 대상 아님 — 같은 입력 10회 수동 실행 후 통과/실패 수기 기록.
- 실제 씬 연결(Training→Match 규칙 전달)이 아직 없으면 7번은 임시 하네스로라도 흐름을 한 번 검증할 것.
